# mattermost-cli

Native Mattermost CLI for agents and humans. The npm package is a small launcher that selects a checksum-bound platform package produced from the same Go release build.

No install script downloads or executes code. npm resolves the matching optional dependency for darwin or linux on arm64 or amd64.

See https://github.com/ardasevinc/mattermost-cli for documentation and release verification.
